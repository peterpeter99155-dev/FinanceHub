# FinanceHub 決策紀錄

## 文件資訊

- 文件狀態：持續更新
- 用途：記錄重要產品與技術決策、原因及影響

---

## DEC-001 產品以淨資產為核心

- 日期：2026-07-27
- 狀態：已確認

### 決定

FinanceHub 的首頁與主要體驗以淨資產為核心，優先回答：

1. 我現在有多少淨資產？
2. 我的資產為什麼發生變化？

### 原因

產品目標是追蹤與理解財富，而不是建立生活消費日誌。

### 影響

- 資產、負債與估值優先於細緻記帳功能。
- 基本支出分類只用於解釋支出變化。
- 不要求完整記錄每筆小額消費。

---

## DEC-002 採抓大放小原則

- 日期：2026-07-27
- 狀態：已確認

### 決定

優先追蹤重要資產、負債、固定支出、大額交易與明顯變化。小額交易可記錄，但不是必要操作。

### 原因

逐筆生活記帳會增加負擔，且對掌握整體財務狀況的價值有限。

---

## DEC-003 所有主要功能保留手動與自訂能力

- 日期：2026-07-27
- 狀態：已確認

### 決定

自動化功能不得成為使用 FinanceHub 的必要條件。未支援或無法辨識的資料，必須允許使用者手動建立、修改與管理。

### 影響

- 手動資料來源是正式功能，不是暫時替代品。
- 系統預設、自訂類型與暫未分類必須分開。
- 自動辨識結果必須允許人工修正。

---

## DEC-004 初期採單一使用者與本機優先

- 日期：2026-07-27
- 狀態：已確認

### 決定

第一階段只服務產品建立者本人，主要在 Windows 電腦使用，財務資料預設保存在本機。

### 原因

先透過真實使用驗證產品價值與操作流程，再評估提供給其他使用者。

### 影響

初期不優先開發：

- 帳號註冊
- 多使用者資料隔離
- 雲端同步
- 訂閱收費
- 家庭共享

---

## DEC-005 基準幣別為新臺幣

- 日期：2026-07-27
- 狀態：已確認

### 決定

首頁與主要報表統一使用新臺幣。美股與其他外幣部位依匯率換算，並保留原幣價值、匯率與更新時間。

### 影響

- 股票市值以目前市場價格計算。
- 股票成本只用於損益，不用於目前總資產。
- 匯率更新失敗時沿用上一筆成功資料並標示過期。

---

## DEC-006 房產與房貸分列

- 日期：2026-07-27
- 狀態：已確認

### 決定

房產以目前估值列為資產；房貸剩餘本金列為負債。

```text
房產淨值 = 房產目前估值 − 房貸剩餘本金
```

房產淨值只作為分析數字，不得再次加入總資產。

---

## DEC-007 銀行自動資料優先使用 Email

- 日期：2026-07-27
- 狀態：已確認

### 決定

銀行資金異動的第一個自動來源使用 Email，不使用 LINE 通知作為 MVP 來源。

### 原因

- Email 內容通常較完整。
- 可以回溯歷史資料。
- 不依賴手機通知權限與顯示狀態。
- 可與信用卡帳單及電子發票共用匯入流程。

---

## DEC-008 採半自動 MVP

- 日期：2026-07-27
- 狀態：已確認

### 決定

MVP 先建立完整的手動財務底座，並加入有限但可實際驗證的自動化：

- 股票／ETF 每日價格更新與立即刷新。
- 一家主要銀行的 Email 通知匯入。
- 一種主要信用卡 PDF 帳單匯入。

### 原因

純手動版本不足以驗證 FinanceHub 的自動監測價值；一次支援所有來源則會使範圍過大。

### 影響

- 自動來源先支援代表格式，再依實際使用逐步擴充。
- 所有匯入結果必須能手動修正。
- 解析失敗或不確定資料進入待確認。

---

## DEC-009 安全功能分階段實作

- 日期：2026-07-27
- 狀態：已確認

### 決定

MVP 先完成會影響底層架構與資料外洩風險的安全設計；操作體驗型安全功能可延後。

### MVP 必須納入

- 資料儲存層預留加密設計。
- 敏感憑證使用可替換的安全儲存介面。
- 真實帳單、Token、密碼與正式資料庫不得提交 Git。
- 未經同意不得將敏感資料傳給外部 AI。
- 正式加密完成前只使用假資料或匿名資料。

### 可延後至 V1

- 完整主密碼與自動鎖定。
- 畫面金額與帳號遮蔽。
- 加密匯出與備份。
- 一鍵清除資料及授權。

---

## DEC-010 採敏捷與垂直切片開發

- 日期：2026-07-27
- 狀態：已確認

### 決定

不等待所有規格完美才開始寫程式。每次先完成一個小而可操作的端到端功能，交由使用者實際體驗，再調整需求與下一個 Sprint。

### 第一個垂直切片

```text
手動建立資產與負債
→ 儲存資料
→ 計算淨資產
→ 首頁顯示資產與負債
→ 修改後重新計算
```

### 影響

- 規格只需足以支援下一個 Sprint。
- 實際使用回饋可以修改尚未穩定的需求。
- 每個 Sprint 都應產出可執行、可驗證的成果。

---

## DEC-011 桌面技術採快速開發方案

- 日期：2026-07-27
- 狀態：已確認

### 決定

第一階段使用以下技術方向：

- Electron：Windows 桌面程式外殼
- React：使用者介面
- TypeScript：主要開發語言
- Electron Forge：專案建立、開發與封裝工具
- Electron Forge Webpack TypeScript 範本：初期建置基礎

資料庫及其他函式庫在對應 Sprint 開始前再決定。

### 原因

- 快速建立財務儀表板、表格與互動介面。
- AI 輔助開發與除錯的資料及範例充足。
- 適合需求尚在迭代的個人桌面產品。
- 未來仍可保留 React 畫面與領域邏輯，評估轉換桌面外殼。

### 已知取捨

- 安裝檔與記憶體使用量通常高於輕量原生方案。
- Electron 具備較高的本機權限，必須遵守安全隔離原則。
- 若未來確實出現效能或體積問題，遷移至其他桌面外殼仍屬正式重構，不是假設零成本。

---

## DEC-012 保持模組分離

- 日期：2026-07-27
- 狀態：已確認

### 決定

至少分離下列責任：

```text
使用者介面
財務領域規則
應用流程
資料存取
外部資料整合
Electron 桌面橋接
```

### 原則

- React 畫面不得直接操作資料庫、檔案系統或敏感憑證。
- 財務計算規則不得依賴 Electron 或特定畫面元件。
- 銀行 Email、信用卡 PDF、股票價格等來源各自透過明確介面接入。
- 桌面能力只透過受限制且驗證輸入的橋接介面提供。
- 不得將完整 Electron 或 Node.js 能力暴露給畫面層。

### 目的

- 降低需求修改的影響範圍。
- 讓財務規則可獨立測試。
- 便於替換資料庫、資料來源或桌面外殼。
- 避免未來加入安全保護時需要全面重寫。

---

## DEC-013 Sprint 01 採一週 timebox

- 日期：2026-07-27
- 狀態：已確認

### 決定

Sprint 01 以一週作為開發與檢視週期。這是開發節奏，不是資料更新頻率或固定發布週期。

### 影響

- Sprint Goal 保持為第一個可操作的淨資產垂直切片。
- 若一週內無法完成全部工作，優先維持可驗證的垂直成果，不擴大範圍。
- Sprint Review 後再根據實際操作結果安排下一個 Sprint。

---

## DEC-014 本機資料庫採 SQLite

- 日期：2026-07-27
- 狀態：已確認

### 決定

第一版採 SQLite 作為本機資料庫，並使用 Electron 所含 Node.js 的內建 SQLite API，避免額外原生 driver 對 Python、C++ 建置工具及 Electron ABI 的依賴。

### 原則

- 財務領域規則不得依賴 SQLite。
- 應用層透過 Repository 介面存取資料。
- schema 變更必須透過可版本化 migration。
- 金額使用整數最小貨幣單位儲存及運算。
- React renderer 不得直接存取資料庫。
- 資料庫尚未加密前，只能使用假資料或匿名化資料。

### 影響

- Electron main process 負責開啟與關閉資料庫。
- preload 只暴露受限制且具型別的應用功能。
- 若未來的加密需求無法由現行方案滿足，資料存取 adapter 必須可替換，不得要求重寫領域規則。

---

## DEC-015 未關聯歷史的手動項目允許永久刪除

- 日期：2026-07-27
- 狀態：已確認

### 決定

尚未關聯交易、估值歷史或其他財務紀錄的手動資產與負債，允許使用者永久刪除。刪除前必須明確提醒此操作無法復原。

已有關聯紀錄的項目不得永久刪除，只能停用。

### 原因

Sprint 01 的手動項目尚無任何歷史關聯，強迫保留誤建或測試資料會增加操作負擔；但未來有交易或估值歷史後，直接刪除會破壞報表及可追溯性。

### 影響

- Sprint 01 畫面以「刪除」取代「停用」。
- 資料存取層保留未來檢查關聯紀錄的責任邊界。
- 後續加入交易與歷史資料時，必須先實作關聯檢查，再決定提供刪除或停用。

---

## DEC-016 名稱可省略，自訂類型排入下一 Sprint

- 日期：2026-07-27
- 狀態：已確認

### 決定

手動建立資產或負債時，名稱為選填。使用者未輸入名稱時，系統以目前選擇的類型名稱作為正式名稱。名稱允許重複，項目仍以唯一識別碼區分。

使用者自訂資產與負債類型依 FR-100 保持為 MVP 正式需求，並列為下一個 Sprint 的前置功能，不擴張 Sprint 01。

### 影響

- 一般使用者可只選類型及輸入金額，減少必要操作。
- 未來選擇自訂類型且名稱留空時，使用該自訂類型名稱。
- 下一個 Sprint 必須支援自訂類型的新增、重新命名及停用。
- 已被項目使用的類型不得直接永久刪除。

---

## DEC-017 Sprint 02 建立手動收支流水

- 日期：2026-07-28
- 狀態：已確認

### 決定

Sprint 02 在淨資產底座上建立手動收入、支出、帳戶轉帳、信用卡消費與信用卡繳款，並提供月度收入、支出與結餘。

可收付款項目先限於現金、銀行帳戶、電子支付／電子錢包及信用卡。房屋增貸、資產質押、投資買賣、貸款與房貸拆分不納入本 Sprint。

### 原因

手動交易是後續定期收支、銀行 Email 與信用卡帳單匯入的共同底座。先驗證人工流程與財務計算，才能避免自動資料造成重複或錯誤餘額。

### 影響

- 期初餘額計入資產但不列為收入。
- 自有帳戶轉帳不列為收入或支出。
- 信用卡消費在消費時認列支出，繳款不得再次認列。
- 手動交易可修改與刪除，帳戶餘額及報表必須同步還原與重算。
- 未來交易、定期收支與預定項目延至後續 Sprint。
- 銀行或信用卡即時 API 暫不列入近期範圍。

---

## DEC-018 財務歷史不設月份或筆數上限

- 日期：2026-07-28
- 狀態：已確認

### 決定

交易總筆數及保存月份不設產品上限，舊資料不自動刪除。畫面依月份查詢，交易列表每次先載入 50 筆並允許繼續載入。

### 原因

個人財務資料需要長期累積，不能要求使用者因使用年限或筆數刪除歷史。效能問題應由索引、分月查詢及分批載入處理。

### 影響

- Repository 必須支援月份條件與分批查詢。
- 月度統計不得依賴畫面已載入的 50 筆資料，必須由完整月份資料正確計算。
- 未來可加入備份、匯出及封存，但封存不得等同刪除。

---

## DEC-019 已使用的自訂類型不得停用或刪除

- 日期：2026-07-28
- 狀態：已確認

### 決定

自訂資產或負債類型只要仍被任何項目使用，就不得停用或永久刪除。使用者必須先刪除關聯項目，或在未來支援改換類型後將項目移至其他類型。

### 原因

停用類型不應導致既有資產或負債突然從財務總覽消失。保護仍在使用的類型，可以避免項目名稱、總額與使用者預期不一致。

### 影響

- 未使用的自訂類型仍可停用或永久刪除。
- 停用或刪除失敗時，畫面必須明確說明該類型仍被資產或負債使用。
- 財務總覽不因類型管理操作而隱藏仍有效的項目。

---

## DEC-020 收支頁顯示帳戶帳面餘額與資金流向

- 日期：2026-07-28
- 狀態：部分由 DEC-022 取代

### 決定

收支頁提供帳戶概況，顯示銀行、現金及信用卡的 FinanceHub 帳面餘額；交易流水必須顯示資金從哪個帳戶扣款、入帳至哪個帳戶，或信用卡待繳的變化。沒有符合交易條件的帳戶時，畫面提供明確原因及前往建立帳戶的操作。

### 原因

交易若缺少帳戶來源便無法核對實際資金流向；只在資產頁顯示餘額也會增加使用者切換與確認成本。現階段尚未串接銀行 API，因此必須避免把手動計算結果誤稱為銀行即時餘額。

### 影響

- 新增、修改或刪除交易後，帳戶概況立即載入更新後的帳面餘額。
- 銀行與現金顯示帳面餘額，信用卡顯示帳面待繳。
- 收支頁缺少帳戶時不得只留下無法操作的「請選擇帳戶」。
- 未來銀行同步餘額必須另外標示資料來源及最後同步時間。

---

## DEC-021 主要記帳介面只顯示收入與支出

- 日期：2026-07-28
- 狀態：部分由 DEC-022、DEC-023 取代

### 決定

主要新增交易介面只顯示「收入」與「支出」。支出的付款方式可選銀行、現金或信用卡；選擇信用卡時，系統內部仍建立信用卡消費並增加待繳。帳戶轉帳與信用卡繳款入口先隱藏，底層能力及既有資料保留。

使用者介面統一使用「信用卡」，不要求一般使用者理解「信用卡負債」；財務領域與計算仍將信用卡餘額視為負債。

### 原因

同時顯示收入、支出、轉帳、信用卡消費及信用卡繳款會提高第一次使用的理解成本。收入與支出是最容易理解的主要心智模型，其他交易可在流程成熟後放入進階功能。

### 影響

- 信用卡支出仍只認列一次，未來繳款不得再次列為支出。
- 既有轉帳與信用卡繳款仍顯示在流水中，但主要介面暫不提供新增或修改入口。
- 帳戶餘額區縮為精簡的銀行與現金帳面餘額，不重複展示信用卡負債總覽。
- 交易流水依日期分組，每筆只顯示時間，帳戶來源及備註使用可閱讀的次要文字尺寸。

---

## DEC-022 收入與支出的帳戶改為選填

- 日期：2026-07-28
- 狀態：已確認

### 決定

一般收入與支出不強制指定帳戶。未指定時只建立收支紀錄及月度統計，不改變資產或負債；有指定銀行或現金時，才同步帳面餘額並在流水顯示資金來源。主要流程暫不提供信用卡付款。

若支出超過所選帳戶的帳面餘額，畫面提示使用者改成不指定帳戶，不允許帳面資產直接變成負數。單筆金額的資料安全上限仍保留。

### 原因

強制選擇付款方式會阻礙快速記帳；完全移除帳戶連動又無法滿足精確管理餘額的需求。選填帳戶能讓使用者依當下需求選擇快速或精確的記錄方式。

### 影響

- 收入顯示「入帳帳戶（選填）」，支出顯示「扣款帳戶（選填）」。
- 預設為「不指定帳戶」。
- 未指定帳戶的交易仍計入當月收入或支出。
- 可用餘額只作為輔助資訊，不能成為建立一般收支的必要條件。

---

## DEC-023 暫停新的信用卡功能

- 日期：2026-07-28
- 狀態：已確認

### 決定

目前介面停止提供新的信用卡建立、信用卡付款、信用卡消費及信用卡繳款入口。既有信用卡與歷史交易繼續顯示，允許使用者刪除，不得因功能暫停而遺失資料。底層信用卡模型保留供未來進階功能使用。

交易流水依日期分組，日期標題顯示「當日收支」，計算方式為當日收入減去當日支出；轉帳與信用卡繳款不納入。

### 原因

信用卡待繳、繳款與一般支出的關係需要額外理解，現階段先專注銀行、現金及一般收入與支出。每日收支能在不增加操作步驟的情況下，幫助使用者快速理解當天資金變化。

### 影響

- 新增負債時不再顯示信用卡類型。
- 既有信用卡在編輯時以「信用卡（既有）」辨識。
- 每日收支正數、負數及零分別使用易辨識的顏色。
- 重複的月份說明區移除，以交易流水中的月份標題為準。

---

## DEC-024 財務增減採用綠增紅減

- 日期：2026-07-28
- 狀態：已確認

### 決定

FinanceHub 採用美股式財務顏色語意：對財務狀況有正向影響的增加使用綠色，負向影響的減少使用紅色，沒有增減或不影響總額使用灰色。編輯與操作狀態使用藍色等非財務色彩。

### 規則

- 收入、資產與正數收支差額使用綠色。
- 支出、負債與負數收支差額使用紅色。
- 負債增加雖然數值增加，但會降低淨資產，因此使用紅色。
- 零值、轉帳及其他不影響收支總額的資料使用灰色。
- 交易收入與支出同時使用 `＋`、`−` 符號，不能只依賴顏色傳達意義。
- 顏色只套用在正負號與數字；`TWD`、標題、類型及其他說明文字維持中性色。
- 淨資產使用淺色卡片，依正負切換邊框及數字顏色，不使用深色底遮蔽財務語意。
- 列表編輯與刪除使用具文字提示及無障礙名稱的鉛筆、垃圾桶圖示；取消編輯使用 X。

### 原因

統一的顏色與正負號能讓使用者更快辨識收入、支出及財務方向；同時保留符號，可避免色覺差異或螢幕顯示造成誤解。

---

## DEC-025 電子支付／電子錢包新增型別

- 日期：2026-07-28
- 狀態：已確認
- 對應：G-01

### 背景

`04_Decision_Log.md` 與 `07_Sprint_02_Plan.md` 都把「電子支付／電子錢包」列為可收付款項目，`TransactionAccountKind` 也有 `electronic_wallet`，但 `FinancialItemType` 沒有對應型別，`mapAccountKind()` 永遠不會回傳它。文件承諾的功能實際做不到，該列舉值是走不到的死路。

### 決議

新增 `electronic_wallet` 到 `FinancialItemType`，補上映射、中文標籤、新增選單與測試。

**執行歸屬：這是新功能，排入 Sprint 03，不得混入 Sprint 2.5。**

**後續狀態（2026-07-30）：Sprint 03 未實作，目前列為後續待排；
不得視為已完成。** 原執行歸屬保留作為歷史紀錄。

---

## DEC-026 轉帳手續費就是普通支出

- 日期：2026-07-28
- 狀態：已確認
- 對應：G-02

### 決議

手續費以一筆普通 `expense` 表示，不成為獨立交易類型。交易類型固定為 5 種。

**後續狀態（2026-08-04）：** DEC-038 新增 `credit_card_refund` 後，
交易類型現為 6 種；本決議「手續費不是獨立交易類型」仍維持不變。
「固定為 5 種」保留為當時歷史內容，不再代表現在的列舉數量。

待辦：修正 `07_Sprint_02_Plan.md` 第 4 節那份寫成 6 項的清單文字，使其與程式一致。（屬文件修正，可在 Sprint 2.5 執行。）

附帶問題「轉帳與手續費是否建立關聯」已於同日決議，見 G-05。

---

## DEC-027 期初餘額不進資料模型

- 日期：2026-07-28
- 狀態：已確認
- 對應：G-03

### 決議

期初餘額只是說明用概念，不新增欄位。實際上就是財務項目在第一筆交易之前的既有金額。

**明文限制：未來不得因為文件出現「期初餘額」字樣，就自行新增 `openingBalance` 欄位或表格。** 若日後需要顯示「期初 + 本期變動 = 現在餘額」，必須先更新本表並取得同意。

---

## DEC-028 資料狀態由使用者與系統分別設定

- 日期：2026-07-28
- 狀態：已確認
- 對應：G-04

### 背景

`DataStatus` 的五個值分別來自 `02_Requirements.md` FR-010（實際、推算、手動確認、待確認）與 `01_Product_Vision.md`（更新失敗時標示資料已過期）。兩份文件的概念被併入同一個列舉，這一步當初未經確認。

### 問題

目前五個值全部放在同一個下拉選單由使用者自選。但 `confirmed`、`pending_confirmation` 是使用者的意思表示，`automatic`、`estimated`、`stale` 是系統的判斷。混在一起會讓使用者手動宣告系統無法驗證的狀態（例如手動選「由系統自動更新」）。且 Sprint 01／02 全為手動輸入，後三者現階段一個都不會真的發生。

### 決議

- 五個值全部保留，資料模型不變。
- **使用者可手動選擇的只有 `confirmed` 與 `pending_confirmation`。**
- `automatic`、`estimated`、`stale` 只能由系統寫入，現階段不出現在任何選單。
- 中文文案統一口吻，見第 6 節。

**執行歸屬：這會改變畫面行為，排入 Sprint 03，不得混入 Sprint 2.5。**

**後續狀態（2026-07-30）：Sprint 03 未實作。S4-01 已將手動輸入
介面只顯示 `confirmed` 與 `pending_confirmation` 納入 Sprint 04；
其餘資料模型規則不變。** 原執行歸屬保留作為歷史紀錄。

---

## DEC-029 轉帳手續費獨立支出並綁定父轉帳

- 日期：2026-07-28
- 狀態：已確認
- 對應：G-05

### 問題來源

使用者刪除一筆寫錯的轉帳後，手續費仍留在帳上，必須再做一次相同的刪除動作。實務上手續費是為了那筆轉帳才發生的，不該獨立存活。

### 決議

手續費維持為一筆獨立的 `expense`（保有自己的分類與流水顯示，不破壞「轉帳不計收支」這條規則），但新增選填關聯欄位 `parentTransferId` 指向所屬轉帳。

**前提修正：手續費並非每次轉帳都有。** 同行轉帳通常免費，網銀多有免費次數，跨行才固定收取。因此手續費欄位一律**選填且預設空白**，不得預設帶入金額。

連動規則（全部必須實作，不得只做刪除）：

1. 刪除轉帳 → 一併刪除其手續費。確認視窗必須寫明將同時刪除的筆數與金額，例如「將同時刪除手續費 TWD 15，共 2 筆」。
2. 刪除手續費 → **不影響**轉帳。連動是單向的。
3. 修改轉帳的轉出帳戶 → 手續費的扣款帳戶跟著改。手續費本來就從轉出帳戶扣。
4. 修改轉帳的財務時間 → 手續費的財務時間跟著改。否則兩筆可能落在不同月份，月度統計會對不上。
5. 修改轉帳金額 → 手續費**不變**。手續費是固定費用，與轉帳金額無關。
6. 手續費的來源帳戶與財務時間不可單獨編輯。可單獨編輯的只有金額、分類與備註。
7. 允許事後補記：對帳時才發現手續費，可為既有轉帳補記一筆並建立關聯。
8. 允許沒有父轉帳的獨立手續費支出（例如帳戶管理費），該情形即為普通支出。

**執行歸屬：Sprint 03。** 這會新增資料欄位、migration 與畫面行為，不得混入 Sprint 2.5。

**後續狀態（2026-07-30）：Sprint 03 未實作，目前列為後續待排；
不得視為已完成。** 原執行歸屬保留作為歷史紀錄。

**同時必須更新的文件（不只術語表）：**

- `02_Requirements.md`：手續費的輸入與連動行為。
- `03_Business_Rules.md`：連動刪除與跟隨規則。
- `07_Sprint_02_Plan.md` US-04 的驗收條件需補充連動說明。

---

## DEC-030 Sprint 02 未完成項移至 Sprint 03

- 日期：2026-07-28
- 狀態：已確認
- 對應：G-06

### 背景

`07_Sprint_02_Plan.md` 要求的兩項功能並未實作，但文件標記為「已完成」。

- 第 9 節的交易篩選（依交易類型或分類）。
- 第 3 節「將既有交易移至替代分類」的畫面流程。底層 `reassignAndDelete` 已存在，但管理畫面刪除時只呼叫一般 `delete`，因此使用中的自訂收支分類無法在畫面上完成移轉。

### 決議

兩項都移至 Sprint 03，並修正 `07_Sprint_02_Plan.md` 的完成狀態與範圍標記，使進度紀錄與事實一致。

**後續狀態（2026-07-30）：Sprint 03 未實作，兩項目前列為後續
待排；不得視為已完成。** 「移至 Sprint 03」保留作為當時的歷史
決議。

### 理由

Sprint 2.5 是純技術整理，不混新功能。把未完成項留在「已完成」的文件裡，會讓後續每一次規劃都建立在錯誤的起點上。

**注意：** 分類重新指派 UI 缺席的實際後果是——**使用中的自訂收支
分類目前無法刪除**。這是功能缺口，不是設計選擇。此項原排入
Sprint 03，但 Sprint 03 未實作，目前列為後續待排。

---

## DEC-031 「類型／分類」UI 文案修正列為 Sprint 2.5 例外

- 日期：2026-07-28
- 狀態：已確認
- 對應：G-07

### 問題來源

本表第 4 節規定「類型」只用於資產／負債，「分類」只用於收入／支出。但目前畫面仍寫「收入類型」「支出類型」「新增類型」，收支表單的欄位也叫「類型」。

這與 `09_Sprint_02_5_Plan.md`「不得改變任何 UI 文案」直接衝突 —— 遵守本表就必須改文案，遵守該前提就不能改。此矛盾由 Sprint 2.5 開工前的規範審查提出，並依 `AGENTS.md` 停止實作、回報處理。

### 決議

**列為 Sprint 2.5 的明確例外，在階段 0 完成。**

### 理由

1. 這是純字串更動，不涉及任何邏輯或版面。
2. E2E selector 會引用這些文字。在階段 1 重建 E2E **之前**改完，selector 只需要寫一次；拖到 Sprint 03 就要改兩次。
3. 若不改，術語表從生效第一天就與程式不一致，後續每次規劃都建立在錯誤的名稱上。

### 範圍限定

- 只改收入／支出相關的「類型」字樣為「分類」。
- 資產／負債的「類型」字樣**不動**，那是正確的。
- 不改任何版面、樣式、欄位順序或互動行為。
- 同時修正所有受影響的 E2E selector。

**Sprint 2.5「不得改變可見行為」的前提，例外僅此一項與錯誤訊息代碼化（T-25），不得再自行擴充其他例外。**

---

## DEC-032 FinanceHub 加密格式 v1

- 日期：2026-07-29
- 狀態：已確認
- 對應：S3-10

### 適用範圍

本決策只定義加密格式，不建立 metadata sidecar、不啟用資料庫加密，也不實作金鑰導出。規格確認後，S3-11 之後的工作才可依此實作。

### 七項固定規格

| 項目 | v1 規格 |
|---|---|
| 格式版本 | `formatVersion = 1` |
| KDF 版本 | `kdfVersion = 1` |
| KDF | `scrypt` |
| KDF 參數 | `N = 262144 (2^18)`、`r = 8`、`p = 1`、`keyLength = 32 bytes`、`maxmem = 536870912 bytes (512 MiB)` |
| salt | 每個資料庫各自使用 CSPRNG 產生 `32 bytes`；sidecar 以有 padding 的標準 Base64 保存 |
| cipher | 明確指定 SQLite3 Multiple Ciphers 的 `chacha20`（sqleet ChaCha20-Poly1305），`legacy = 0`、`plaintextHeaderSize = 0`、`hmacCheck = 1`、`keyMode = raw` |
| cipher page size | `4096 bytes` |
| 密碼編碼 | 不 trim；先做 Unicode `NFC` 正規化，再編碼為 UTF-8 |

格式 v1 的密碼技術限制為：正規化後不得為空、最多 1024 個 Unicode scalar values，且 UTF-8 結果最多 4096 bytes。產品層日後可以要求更高的最低強度，但不得放寬這兩個格式上限，也不得改變既有格式版本的正規化與編碼方式。

產品層的 v1 密碼規則為：

- 至少 8 個 Unicode scalar values。
- 不強制大小寫、數字、符號或特定字元種類。
- S3-20 必須顯示「建議使用多個不相關詞組成的密語」或語意等同的引導文字。

8 字元只是最低可接受門檻，不代表 8 字元密碼足以抵抗離線破解。攻擊者取得資料庫與 sidecar 後可以離線猜測；常見、可預測或重複使用的弱密碼即使經過 scrypt 仍可能被破解。繼續提高 KDF 成本會同步增加正常使用者的解鎖等待與記憶體壓力，邊際效益有限；增加不可預測的密碼長度才是更有效的改善方式。因此 FinanceHub 採「鼓勵較長密語、不強制複雜度」：避免使用者為符合字元規則形成可預測模式，也避免日後被誤改為強制大小寫／數字符號的規則。

### KDF 參數選擇理由

#### `N = 262144 (2^18)`

- OWASP 對 scrypt 的目前最低建議是 `N = 2^17`、`r = 8`、`p = 1`。FinanceHub v1 提高一級到 `2^18`，增加離線猜密碼的 CPU 與記憶體成本。
- scrypt 的主要記憶體成本約為 `128 × N × r`，本設定約為 256 MiB。
- 2026-07-29 在目標開發機（Intel Core Ultra 7 265K、32 GB RAM、Node.js 24.15.0、Windows x64）以相同密碼、16-byte 固定測試 salt、`r = 8`、`p = 1`、`keyLength = 32` 各暖機一次後測量五次：

| N | 估計記憶體 | 五次範圍 | 中位數 |
|---|---:|---:|---:|
| `2^16` | 64 MiB | 90.4–91.5 ms | 90.9 ms |
| `2^17` | 128 MiB | 179.8–184.3 ms | 180.6 ms |
| `2^18` | 256 MiB | 368.4–382.8 ms | 370.5 ms |

約 371 ms 的導出只在建立或解鎖資料庫時執行一次，不在每次查詢執行。它在目標機上仍屬可接受的互動等待，同時不會像 90–180 ms 那麼容易被大量嘗試。低階支援機器仍需在實作驗收時量測；若 v1 在最低支援硬體不可接受，只能在尚未產生正式 v1 資料前修訂本決策，不能讓程式執行時自行降級參數。

#### `r = 8`

採用 OWASP 建議的 block size。它是廣泛使用且已有互通測試基礎的值；任意提高 `r` 會再增加記憶體與時間，卻不如以版本化的 `N` 明確表達成本升級。

#### `p = 1`

桌面解鎖是單次互動工作，不用提高平行化參數來倍增成本。`p = 1` 也符合 OWASP 最低建議，避免在核心數較少的電腦造成不穩定延遲。

#### `keyLength = 32`

輸出 256-bit 主金鑰。格式 v1 再以 HKDF-SHA-256 和不同的固定 `info` 分別導出 32-byte 資料庫金鑰與 32-byte verifier 金鑰，讓兩個用途有明確的 domain separation，不重複使用同一把金鑰：

- 資料庫金鑰：`info = "FinanceHub/database-key/v1"`
- verifier 金鑰：`info = "FinanceHub/key-verifier-key/v1"`

HKDF 的 salt 使用長度為 0 的 byte sequence；scrypt 已使用每個資料庫獨立的 salt 產生 pseudorandom 主金鑰，HKDF 在此只負責用途分離。

#### `maxmem = 512 MiB`

`maxmem` 是執行上限，不是固定配置量。v1 的估計工作記憶體約 256 MiB；512 MiB 為實作額外配置保留餘裕，同時阻止程式錯誤讓單次導出無上限耗盡記憶體。sidecar 只提供 `kdfVersion`，程式必須用它查詢內建的可信參數表，不接受外部提供的 KDF 參數。

#### 非同步執行

實作必須使用非同步 `scrypt`，不得使用 `scryptSync`。目標機的一次 v1 導出約需 371 ms；若在 Electron main process 同步執行，期間會阻塞事件迴圈，使視窗、IPC 與作業系統事件看起來無回應。非同步執行不降低 KDF 成本，只避免把計算等待轉成介面凍結。

#### 記憶體清除的能力邊界

- 使用者主密碼必須只透過單一解鎖 IPC 一次性傳入 main process；不得回傳、保存、記錄或再跨 IPC 傳送。
- JavaScript 字串是 immutable，無法可靠覆寫或清零。程式能做的是不保存主密碼引用並在導出完成後立即離開作用域；不得宣稱密碼已從記憶體、swap、crash dump 或原生函式庫副本完全消失。
- 密碼 UTF-8 bytes、scrypt 主金鑰、HKDF 子金鑰、raw-key 暫存 Buffer 在使用後必須以 `fill(0)` 做 best-effort 清零，但同樣不能保證執行環境或原生函式庫沒有其他副本。
- 密碼、主金鑰與子金鑰不得出現在檔案、log、錯誤訊息、IPC 回傳值或診斷輸出。

#### salt：32 bytes、CSPRNG

- 每個資料庫獨立產生，避免兩個使用相同密碼的資料庫得到相同主金鑰，也阻止預先計算表重用。
- 32 bytes 高於計畫要求的至少 16 bytes，碰撞機率可忽略，成本也極低。
- 必須使用作業系統支援的密碼學安全亂數來源，例如 Node.js `crypto.randomBytes(32)`；不得使用時間、UUID、`Math.random()` 或可預測值。
- salt 不是秘密，明文放在 sidecar 是格式的一部分。

### cipher 規格與選擇理由

格式 v1 必須逐項套用並驗證以下設定，不能依賴套件預設：

| 參數 | 固定值 | 理由 |
|---|---|---|
| `cipher` | `chacha20` | SQLite3 Multiple Ciphers 對新資料庫推薦的現代方案；相較軟體實作的 AES-CBC 通常有較好的跨硬體效能。 |
| `legacy` | `0` | 新資料庫不使用相容舊格式。 |
| `keyMode` | `raw` | 傳入從 scrypt 主金鑰經 HKDF-SHA-256 導出的 32-byte 資料庫子金鑰，不再把使用者密碼直接交給 cipher 的 PBKDF2。 |
| `plaintextHeaderSize` | `0` | 不為平台相容性留下明文資料庫標頭。 |
| `hmacCheck` | `1` | 讀取時不得關閉 authentication tag 驗證。復原工具若需例外，必須是獨立、明確授權的流程，正式應用不得有開關。 |
| `pageSize` | `4096` | SQLite 與此 cipher 的標準頁面大小；在小型個人財務資料庫兼顧隨機讀寫與每頁加解密成本。 |

raw key 模式繞過 cipher 內建的 PBKDF2，因此 `kdf_iter` 不參與格式 v1 的金鑰導出；唯一密碼 KDF 是上表固定的 scrypt v1。實作不得在 raw key 失敗時退回 passphrase 模式。

ChaCha20-Poly1305 為每個資料庫頁面從主金鑰、頁碼與 16-byte nonce 導出 one-time key，並為每頁保存 16-byte Poly1305 authentication tag；每頁共保留 32 bytes。這表示它提供 **page 層級的機密性與竄改偵測**，讀取時 tag 不符必須失敗，不得回傳未驗證資料。這能滿足 S3-14 對資料庫 page 的完整性要求，但不保護資料庫外的 metadata sidecar，也不單獨處理整個檔案被回滾成較舊有效版本的情況；sidecar 驗證與備份／回滾風險必須另行處理。

### 密碼轉成 KDF 輸入的唯一流程

1. 接收使用者原始字串；前後空白是密碼內容的一部分，**不得 trim**。
2. 以 Unicode NFC 正規化。
3. 以 Unicode scalar values 計數：不得為空，最多 1024 個。
4. 以 UTF-8 編碼；結果最多 4096 bytes。
5. 將 UTF-8 bytes 與 sidecar 的 32-byte salt 交給 `scrypt v1`，得到 32-byte 主金鑰。
6. 依固定 HKDF-SHA-256 `info` 分別導出資料庫與 verifier 子金鑰。
7. 原始字串、正規化字串、UTF-8 bytes、主金鑰與兩把子金鑰都不得寫入 log、sidecar、資料庫或錯誤訊息。

先 NFC 再 UTF-8 可讓視覺相同但原本分解方式不同的文字得到同一組 bytes。上限同時限制 Unicode scalar values 與 bytes，避免 JavaScript UTF-16 code unit、組合字元及多 byte 字元造成跨實作歧義。

### Metadata sidecar v1

- 固定檔名：`financehub.db.metadata.json`
- 與 `financehub.db` 位於同一目錄。
- 格式：UTF-8 JSON、無 BOM、LF 換行、檔案結尾一個 LF。
- JSON 根節點必須是 object；所有欄位必須存在，未知欄位一律拒絕，避免舊程式忽略新格式的重要安全語意。
- 數字欄位使用 JSON number；版本必須是正整數。
- sidecar 是不可信輸入。程式必須先以固定大小上限讀取，再驗證 schema；只能用其版本編號查詢內建可信表，不得從 sidecar 接受成本參數。

實際檔案結構如下。範例值是假資料；`salt.value` 與 `keyVerifier.value` 解碼後都必須恰好 32 bytes。sidecar 只宣告版本，不保存算法名稱、KDF 成本或 cipher 參數：

```json
{
  "formatVersion": 1,
  "kdfVersion": 1,
  "salt": {
    "encoding": "base64",
    "value": "AAECAwQFBgcICQoLDA0ODxAREhMUFRYXGBkaGxwdHh8="
  },
  "keyVerifier": {
    "version": 1,
    "algorithm": "HMAC-SHA-256",
    "encoding": "base64",
    "value": "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="
  }
}
```

程式必須以內建的 `(formatVersion, kdfVersion)` 可信規格表取得算法、完整 KDF 參數、密碼編碼規則、cipher 與 page size。sidecar 不提供可被攻擊者調低的成本參數；版本未知或不支援時只能明確報錯並停止，不得猜測、降級或採用替代值。

`keyVerifier v1` 固定為：

1. 使用上述 verifier 子金鑰。
2. 對 UTF-8 固定訊息 `FinanceHub/key-verifier/v1` 計算 HMAC-SHA-256。
3. 將 32-byte結果以有 padding 的標準 Base64 存入 `value`。
4. 驗證時必須採 constant-time comparison。

這裡只為了讓 sidecar v1 的實際欄位與格式沒有未定值；產生、驗證、錯誤分類與測試仍屬 S3-12，本項不實作。

### 兩種不可復原的情況

以下兩種情況具有同等嚴重性，FinanceHub 都沒有後門或替代解鎖方式：

1. **忘記主密碼。** 無法重新導出正確金鑰，資料永久無法解密。
2. **遺失或損壞 metadata sidecar。** scrypt salt 只存在 `financehub.db.metadata.json`；沒有原始 32-byte salt，即使記得完全正確的主密碼也無法重新導出同一把金鑰，資料同樣永久無法解密。重新產生 salt、猜測 salt 或建立新的 sidecar 都不能復原原資料。

因此資料庫與 sidecar 是不可分割的備份單位。S3-20 的設定密碼警告必須同時明確說明「忘記密碼」與「遺失 metadata」都會造成永久資料遺失；S3-32 的 README 備份／還原說明也必須涵蓋相同警告，並要求每次備份同時保存 `financehub.db` 與 `financehub.db.metadata.json`。不得只用「請妥善保管」等模糊文字帶過。

### 格式與 KDF 的演進規則

1. `formatVersion` 管理 sidecar 結構、密碼編碼規則、cipher、page size、key mode 等整體格式；任一項改變都建立新的 format version。
2. `kdfVersion` 管理 KDF 算法與完整參數組。提高 `N`、改 `r/p/keyLength/maxmem` 或更換算法都必須新增 KDF 版本，禁止修改既有版本定義。
3. 新版程式內保留所有仍支援的 KDF version registry。開啟既有資料庫時，讀取其版本並選擇該版本的固定參數；**既有資料庫永遠繼續使用建立時的版本**。
4. 建立新資料庫只使用當時最新的 format 與 KDF 版本。
5. sidecar 的格式或 KDF 版本高於程式支援範圍、版本未知或 schema 不符時，明確報錯並停止；不得猜測、降級、改寫或嘗試所有版本。
6. 新增 KDF 版本的流程必須包含：決策紀錄、目標與最低支援硬體 benchmark、已知向量測試、舊版本開啟測試，以及新的固定假資料相容性 fixture。
7. 單純升級應用程式不得偷偷把既有資料庫換成新 KDF。若未來要升級既有資料庫，必須由使用者明確觸發，以已驗證的舊密碼解鎖後，建立新 salt、以新版本重新導出金鑰，透過原子遷移／重加密流程成功完成後才替換舊檔；失敗時原資料庫與 sidecar 必須保持可用。
8. 只有在確認所有使用該舊版本的資料都已安全遷移，且產品提供明確的淘汰與復原方案後，才能停止支援舊 KDF 版本。

### 規格依據

- OWASP Password Storage Cheat Sheet：scrypt 最低建議為 `N = 2^17`、`r = 8`、`p = 1`。
- SQLite3 Multiple Ciphers 官方文件：推薦 `chacha20`；每頁使用 nonce 與 Poly1305 authentication tag，並支援 raw 32-byte key。
- SQLite3 Multiple Ciphers 官方文件：`hmac_check` 預設可被關閉，因此 FinanceHub 必須明確固定為啟用，不能依賴預設。

### S3-14 實測結果

2026-07-29 使用 `better-sqlite3-multiple-ciphers` 12.11.1、Electron 42 ABI 146 對格式 v1 實測：

- 加密連線回報 `cipher = chacha20`、`hmac_check = 1`、`page_size = 4096`。
- 在保持 WAL 連線開啟時寫入 UTF-8 已知字串「示範銀行存款」，實際取得 `financehub.db`、`financehub.db-wal`、`financehub.db-shm`，並一併列舉所有名稱含 `journal` 的相關檔案。逐檔以原始 bytes 搜尋該 UTF-8 byte sequence，結果為 0 筆。
- 關閉資料庫後翻轉 `financehub.db` 第一頁 offset 200 的一個 bit，再以正確主密碼開啟；key verifier 通過，但第一次讀取 `sqlite_master` 失敗，FinanceHub 回傳穩定代碼 `DATABASE_UNREADABLE`，沒有回傳未驗證資料。

因此格式 v1 的 page authentication 在目前目標套件與平台上確實生效。此結果只涵蓋資料庫、WAL、SHM 與 journal；metadata sidecar 仍需依其獨立 verifier／schema 規則處理，不能把 page authentication 誤認為整個備份集合的防回滾機制。

---

## DEC-033 Sprint 03 加密實作與資料存放決議

- 日期：2026-07-29
- 狀態：已確認
- 對應：S3-30～S3-33

### 1. Electron 由 43 降至 42

`better-sqlite3-multiple-ciphers` v12.11.1 在 Windows x64 提供的
Electron 預編譯檔最高為 `electron-v146`；ABI 146 對應 Electron
42。Electron 43 需要 ABI 148，但目標開發機沒有完整的 C++ 原生
模組編譯工具鏈。

因此 FinanceHub 暫時由 Electron 43 降至 Electron 42，直接使用
套件提供且已驗證雜湊一致的預編譯檔。不得以來源不明的 binary
或跳過 ABI 檢查作為替代方案。

### 2. Electron 42 EOL 是有期限的技術債

Electron 42 的官方安全更新支援截止日為 **2026-10-20**。這不是
永久版本選擇，而是必須在期限前處理的技術債。

升版路徑依序為：

1. 若資料庫套件提供 Windows x64 ABI 148 或更新版本的可信
   預編譯檔，驗證相容性 fixture、打包與 Electron 測試後升回。
2. 若沒有預編譯檔，必須評估安裝與維護 C++ 工具鏈，或改採
   經審查的檔案層加密方案。

不得在 EOL 後無期限繼續使用 Electron 42，也不得為了升版關閉
資料庫加密或完整性驗證。

### 3. 加密格式 v1

加密格式全文以本文件 **DEC-032 FinanceHub 加密格式 v1** 為唯一
規格來源，並在本決議再次確認其實作內容：

- `formatVersion = 1`、`kdfVersion = 1`。
- 非同步 scrypt：`N = 262144`、`r = 8`、`p = 1`、
  `keyLength = 32 bytes`、`maxmem = 512 MiB`。
- 每個資料庫使用 CSPRNG 產生 32-byte salt。
- HKDF-SHA-256 分離資料庫金鑰與 verifier 金鑰。
- cipher 為 `chacha20`，`legacy = 0`、
  `plaintextHeaderSize = 0`、`hmacCheck = 1`、
  raw key、page size 4096。
- 密碼不 trim，先做 Unicode NFC，再編碼為 UTF-8。
- sidecar 只保存格式版本、KDF 版本、salt 與 key verifier；
  KDF/cipher 參數只由程式內建可信版本表提供。

任何參數調整都必須依 DEC-032 的版本演進流程建立新版本，不得
修改既有 v1 定義。

### 4. 主密碼最小長度為 8

產品層最低長度固定為 8 個 Unicode scalar values，不強制大小寫、
數字或符號，並引導使用多個不相關詞組成的較長密語。

未來不能直接提高既有解鎖畫面的最低長度：既有使用者可能已使用
符合舊規則但短於新門檻的密碼，直接提高會在正確密碼下仍把使用者
鎖在資料之外。調高前必須先設計「舊密碼仍可解鎖、解鎖後引導更換」
的過渡流程。

### 5. 不轉換 `financehub.dev.db`

舊的 `financehub.dev.db` 只包含開發假資料，因此不建立只會使用
一次、之後需永久維護的轉換 migration。新版使用
`financehub.db` 與 `financehub.db.metadata.json`，不讀取、不修改
舊檔。使用者確認不需要假資料後，可在 FinanceHub 完全關閉時
手動刪除舊檔。

若未來出現含真實資料的舊格式，不能套用本例外；必須另行設計、
測試並記錄可復原的遷移流程。

### 6. 記憶體清理的實際界線

JavaScript 字串不可變，無法可靠覆寫或清零。FinanceHub 能做到的
是讓主密碼只跨一次解鎖 IPC、不保存或記錄、導出後盡快移除引用。
密碼 bytes、主金鑰與子金鑰等可控 Buffer 會以 `fill(0)` 做
best-effort 清零。

不得宣稱 JavaScript runtime、原生函式庫、swap 或 crash dump 中
完全不存在副本，也不得把無法保證的「安全清除」寫成產品承諾。

### 7. 資料存放原則

開發、測試、範例與 Git fixture 一律只使用假資料或匿名化資料。
正式版可以保存真實財務資料，但只建議存放在使用者自己擁有並管理
的電腦上。

檔案加密能保護遭竊裝置、外洩備份與雲端同步檔案，不能防禦持有
電腦系統管理權限的一方。公司、學校或他人所有的電腦管理者仍可能
複製檔案、讀取備份或以監控軟體記錄密碼，因此這些裝置只能用
假資料試用。

---

## DEC-034 新主密碼限制與舊資料相容性

- 日期：2026-07-30
- 狀態：已確認
- 取代範圍：DEC-032 與 DEC-033 中「新設定密碼可使用任意 Unicode、
  不限制字元種類」的產品規則；既有加密格式與導出流程不變。

### 決議

新建立資料庫時，主密碼必須符合：

- 長度至少 8、最多 64。
- 只允許半形可列印 ASCII，也就是半形英文字母、數字及半形特殊
  符號。
- 不允許中文、其他 Unicode 文字、全形字元、空白或換行。
- 英文、數字與特殊符號可以任意組合，不強制三種類型都必須出現。
- renderer 應阻止不符合範圍的內容進入首次設定欄位；main 建立新
  資料庫前仍須獨立驗證，不能把 renderer 當成安全邊界。

這是新密碼的產品限制，不改變加密格式 v1 的 NFC 與 UTF-8 導出
規格。既有資料庫解鎖必須繼續接受原本的 Unicode 密碼，否則更新
程式會把使用舊規則的使用者永久鎖在資料之外。未來的修改密碼功能
應只對新密碼套用本規則。既有資料庫解鎖仍保留 v1 原本的 1024
上限，避免曾設定較長密碼的使用者因更新程式而無法解鎖。

### 介面

- 密碼欄位以眼睛圖示切換顯示與隱藏，不顯示額外文字。
- 空白欄位直接提示至少 8 個半形英文、數字或特殊符號。
- 首次設定畫面顯示實際資料位置，以及必須一起備份的
  `financehub.db` 與 `financehub.db.metadata.json`。
- 警告使用一般使用者可理解的文字說明：FinanceHub 不保存密碼，
  忘記密碼或遺失任一資料檔案都無法復原。

---

## Migration 歷史例外與紀律

- Migration 4 同時包含 schema 變更與預設分類寫入，為技術架構規範建立前的歷史例外，保留原狀，不得修改或拆解。
- Migration 5 與 Migration 4 的預設分類 `INSERT` 內容相同；它是因舊版介面曾允許刪除預設分類而建立的資料修復 migration，保留原狀。
- 已套用的 migration 不得修改，只能新增。此規則正式生效；範圍以 `00_Architecture_Rules.md` 第 5 節為準。

---

## DEC-035 可讀備份名稱、完整匯出與還原延後

- 日期：2026-07-31
- 狀態：已確認

### 決議

1. 新建立的備份目錄使用
   `FinanceHub-backup-YYYY-MM-DD_HH-mm-ss-<UUID>`，日期時間以
   `Asia/Taipei` 顯示，UUID 仍負責唯一性。既有
   `backup-<UUID>` 備份必須繼續可辨識、驗證與保留。
2. 目錄內的 `financehub.db`、`financehub.db.metadata.json` 與
   `manifest.json` 不改名，manifest v1 也不因此改版。
3. 「匯出最新備份」只能匯出最新一份通過既有格式與 SHA-256
   驗證的完整三件式備份。renderer 只能發出窄 IPC 意圖；資料夾
   選擇、檔案複製與匯出後驗證都在 main／infrastructure 完成。
4. 不以「開啟正式資料位置並讓使用者自行複製」作為主要備份流程，
   避免程式運作期間複製到不一致的 DB／WAL 狀態。
5. 一鍵還原不在本次調整實作，列入後續 Sprint。未來還原仍須遵守
   Sprint 04 Review 第 12 節的相容性與安全承諾。

---

## DEC-036 降低備份保留份數時立即確認與清理

- 日期：2026-07-31
- 狀態：已確認

### 決議

1. 增加保留份數，或新值不低於現有有效備份數時，直接保存設定。
2. 新值低於現有有效備份數時，必須先顯示將移除的實際份數及最舊
   備份時間；使用者確認後才立即清理超額備份並保存新設定。
3. 取消確認時，下拉選單維持原值，不得提前保存設定。
4. 清理失敗時不得假裝新設定已完整套用；保留原設定並顯示安全錯誤。
5. application 層必須要求明確的確認參數，不能只依賴 renderer
   是否顯示過視窗。
6. 立即清理沿用既有格式、雜湊、reparse point、quarantine 與
   目錄替換競速防線。

本決議是 Sprint 04「只在新備份成功後清理」的明確後續修正。理由
是避免使用者把保留份數由 30 降為 3 後，下一次按「立即備份」才
突然一次刪除多份，使設定行為與備份行為的因果關係混在一起。

---

## DEC-037 一般介面的備份保留份數列為後續簡化

- 日期：2026-07-31
- 狀態：已確認，後續待排

### 決議

一般使用者需要多版本備份，但不一定需要自行判斷應保留 3、7、14
或 30 份。後續介面優化預計固定顯示並保留最近 7 份，隱藏一般介面
的份數選單；底層可調整能力暫時保留，供測試及未來進階設定評估。

本項目前只列入後續優化，不在本輪文件工作修改程式。等一鍵還原與
備份清單完成後，再依實際使用方式確認是否需要提供進階選項。

---

## DEC-038 信用卡退款與雙餘額模型

- 日期：2026-08-04
- 狀態：已確認

### 決議

1. 新增正式交易類型 `credit_card_refund`，金額保持正數，退款不是
   收入，並使用支出分類。
2. 原消費關聯為選填；退款可在找不到原消費時獨立成立。刪除原消費
   只清空關聯，不刪除退款，也不自動修改原消費。
3. 信用卡採「應繳餘額＋溢繳餘額」雙餘額，兩者皆非負且同一張卡
   最多只有一個大於零。應繳列入總負債，溢繳列入總資產。
4. 退款先減應繳、超出部分進溢繳；消費先耗用溢繳、不足部分增應繳；
   繳款先減應繳、超額部分進溢繳。
5. 舊信用卡資料升級後，原 `amount` 成為應繳餘額，溢繳為 0；升級
   前後總資產、總負債與淨資產必須完全一致。
6. 月統計顯示本月收入、本月支出、本月退款與本月淨收支；本月淨
   收支為收入減支出加退款。

### 原因

真實退款或超額繳款可能使發卡機構反過來欠持卡人。禁止餘額越過零
會迫使使用者建立不存在的消費或負債；雙餘額能保持非負 UI，同時
忠實表達資產與負債方向。

---

## DEC-039 Sprint 05 採來源中立待確認流程，先接一種文字型 PDF

- 日期：2026-08-04
- 狀態：已確認

### 決議

1. 將來源觀察與正式交易分開，資料模型允許多個來源觀察連結同一筆
   正式交易。
2. 所有匯入先建立待確認候選；使用者只能建立新交易、連結既有交易
   或排除。不得自動合併、覆蓋、修改或刪除既有正式交易。
3. Sprint 05 第一個來源是永豐信用卡月結帳單的文字型 PDF。CSV
   保留為後續來源；掃描型 PDF、OCR、兩種 Email parser、Email
   收取與即時通知狀態不納入本 Sprint。
4. 原始 PDF、完整抽取文字與 PDF 開檔密碼不保存。所有處理完全在
   本機；PDF 密碼與 FinanceHub 主密碼必須在程式、錯誤及文件中
   明確區分。
5. 開發與自動化測試只使用自行製作的虛構 PDF。真實帳單只由使用者
   在自己的電腦做不留檔驗收，不得複製、提交或寫入 log。
6. 帳單總額只用於核對，不建立交易；差異只能提示使用者檢查，不能
   產生虛構交易湊平。

### 未來相容性

信用卡每日通知、月結帳單與簽帳金融卡即時通知是三種獨立解析格式。
未來即時通知與月結資料不一致時，只能提出可能配對；簽帳金融卡的
直接扣款語意也不得套用信用卡未結帳模型。

---

## DEC-040 S5-00 PDF 技術原型結論

- 日期：2026-08-04
- 狀態：技術可行性已確認，尚未進入正式產品

### 證據

- 隔離原型使用 PDF.js `6.2.108`、Electron `42.7.1`、Windows x64，
  完成加密中文文字型 PDF 的離線抽取、錯誤密碼、安全失敗、package、
  make 與安裝版驗證。
- 使用自行產生的虛構 PDF 建立跨頁、外幣、退款／扣抵、零元、表尾
  總計、摘要換行與不支援版面測試；摘要跨兩行只有在欄位一致、片段
  恰好兩個、來源索引連續且未被其他交易使用時才合併。
- 使用者在本機以未複製、未保存的真實帳單完成國內與外幣版面驗收；
  repo、fixture、文件、log 與建置產物不包含真實帳單內容。
- 原型容量的可核對 A/B 為：`app.asar` 從 `69.83 MiB` 精簡為
  `40.06 MiB`，原型 packaged 目錄從 `424.27 MiB` 精簡為
  `394.50 MiB`，兩者都減少 `29.77 MiB`；目前精簡原型 Setup 為
  `151.36 MiB`。這些是獨立原型的資產精簡數字，不是正式 FinanceHub
  加入 PDF.js 前後的 production 增量。
- 先前討論中的 `21.61 MiB` 沒有保留同一 FinanceHub HEAD 與相同打包
  設定的前後產物，無法回頭核對，因此不採為正式結論。正式容量增量
  必須在 production integration 後做不含／包含 PDF 資產的 A/B。
- 不得為縮小容量改用有已知安全風險的舊版、連網下載資產或放寬
  Electron 安全設定。

### 限制

原型位於被 Git 忽略且不進入 production bundle 的隔離目錄，不能
視為正式 parser、正式交易匯入或 Sprint 05 已完成。正式實作仍需
依分層規範重建、使用虛構 fixture，並完成 production package 與
安裝端驗收。

---

## DEC-041 Sprint 05 開工決策

- 日期：2026-08-04
- 狀態：已確認

### 決議

1. 有限度恢復信用卡建立及匯入時選擇信用卡；不恢復完整進階信用卡
   操作入口。
2. 完全相同 PDF 以 `sourceFileDigest` 直接阻止重複；跨來源只以
   `observationFingerprint` 提出疑似配對並顯示欄位差異，不使用不透
   明百分比，不自動合併或刪除。
3. 同一次批次確認中的建立、連結與排除全批成功或全批回復。這是
   persistence 原子性；另有一項已確認規則是整批財務預檢不受輸入
   順序影響，兩者不得混為同一保證。
4. 不保存原始 PDF、完整抽取文字、PDF 密碼、完整路徑或原始檔名。
   保存來源種類、帳單月份、信用卡關聯、匯入時間、檔案摘要、parser
   名稱／版本、頁碼／匿名列位置、必要解析欄位、警告與使用者決定。
5. PDF.js 安全版本原則接受。正式整合後以同一 HEAD、相同設定比較
   不含／包含 PDF 資產的 `app.asar`、packaged 目錄、Setup 與安裝後
   空間；Setup 增量不超過 `30 MiB` 可採用，超過即停止並重新審查。
   不得以舊版、連網資產或放寬 Electron 安全設定換取容量。
6. PDF 只有日期時記錄財務時間精確度 `date`；內部可用 Asia/Taipei
   12:00 作技術值但 UI 不顯示。未來可由已配對且提供時間的來源觀察
   提議補齊，仍須使用者確認。
7. 無可靠分類時允許以「暫未分類」建立正式交易；它與「其他」不同，
   並在分類統計中獨立顯示。
8. 帳單明細結尾的銀行合計作為 `statementDetailTotal` 核對基準，
   永遠不是交易。解析加總不一致時禁止批次確認、顯示差額，不得建立
   虛構交易湊平。
